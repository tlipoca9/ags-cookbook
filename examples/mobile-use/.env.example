# =============================================================================
# AgentSandbox Mobile Automation Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Configuration priority (high to low):
#   1. Environment variables
#   2. .env file
#   3. Default values in code
# =============================================================================

# -----------------------------------------------------------------------------
# Required Configuration
# -----------------------------------------------------------------------------

# Your AgentSandbox API Key (required)
E2B_API_KEY=your_api_key_here

# Service domain (required)
# Available regions:
#   - ap-guangzhou.tencentags.com  (Guangzhou)
#   - ap-shanghai.tencentags.com   (Shanghai)
#   - ap-beijing.tencentags.com  (Beijing)
E2B_DOMAIN=ap-guangzhou.tencentags.com

# Sandbox template name (required)
SANDBOX_TEMPLATE=mobile-v1

# -----------------------------------------------------------------------------
# Optional Configuration (used by both quickstart.py and batch.py)
# -----------------------------------------------------------------------------

# Sandbox timeout in seconds (default: 3600 = 1 hour for quickstart, 300 for batch)
# SANDBOX_TIMEOUT=3600

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Batch Operations Configuration (batch.py only)
# -----------------------------------------------------------------------------

# Total number of sandboxes to create (default: 2)
# SANDBOX_COUNT=2

# Number of processes for parallel execution (default: 2)
# PROCESS_COUNT=2

# Thread pool size per process (default: 5)
# THREAD_POOL_SIZE=5

# Whether to use mounted APK (default: false)
# Set to true if COS disk is mounted to sandbox, APK will be read from mount path
# Set to false to upload APK from local machine
# USE_MOUNTED_APK=false
